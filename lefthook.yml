# Lefthook configuration for usync
# See: https://github.com/evilmartians/lefthook/blob/master/docs/configuration.md

glob_matcher: doublestar

pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "**/*.go"
      run: gofmt -w {staged_files}
      stage_fixed: true
    vet:
      glob: "**/*.go"
      run: make vet
    lint:
      run: make lint
    gitignore-check:
      run: make gitignore-check

pre-push:
  commands:
    test:
      run: make test
    build:
      run: make build
    gitignore-check:
      run: make gitignore-check
